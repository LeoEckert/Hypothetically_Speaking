# Optional override — in dev this always falls back to http://localhost:8000,
# in a production build it falls back to the deployed Nebius backend
# hardcoded in src/lib/api.ts. Only set this to point at a different backend.
VITE_API_BASE_URL=
